// Devbox — Presentation / Demo layout
// Single clean pane, minimal UI
layout {
    default_tab_template {
        pane size=1 borderless=true {
            plugin location="zellij:status-bar"
        }
        children
    }

    tab name="demo" focus=true {
        pane name="main" focus=true
    }
}
