# Product context

## Product

- Name: devbox
- One-sentence purpose: observable, governable developer sandboxes and network labs for AI coding agents.
- Primary audience: developers running coding agents against local projects.
- Product repository: this checkout.
- Current release/build: v4 branch, `c64bf826173c` plus reviewed working-tree implementation.

## Durable product truth

- Core jobs: isolate filesystem changes, observe behavior, enforce egress, operate routed labs and ZTP fabrics.
- Demonstrable differentiators: continuous collector; real flow pcap; live topology/traffic view; zero-touch provisioning.
- Claims requiring approval: none for this README still; it shows only locally rendered product UI.
- Terminology: devbox, box, Activity, Policy, Labs, ZTP.

## Demo environment

- Supported OS: local macOS host; product page served on loopback.
- Required account/license: none.
- Required permissions: loopback bind and in-app browser screenshot.
- Network dependencies: none after the build; all assets are embedded.
- Fixture method: built-in scenario catalogue; no user box or account data shown.
- Clean reset: reopen `/labs` in a fresh local tab.
- Known nondeterminism: action timestamps are avoided; scenario catalogue is deterministic.

## Visual system

- Preferred theme: product default dark UI.
- Stable viewport: 1440 × 1000 CSS pixels.
- Brand assets: embedded application CSS.
- Sensitive regions: dashboard/project paths and browser chrome; capture only page content on `/labs`.
