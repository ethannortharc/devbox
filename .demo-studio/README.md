# Product demo workspace

Product-owned plans and verification for reproducible devbox media. Campaign
assets are separated under `source/`, `capture/`, and `exports/`; README-ready
files live under `docs/`.
